# Workspace Rules

## Python Tooling
- Always use `uv` for python package management, installing dependencies, and running python scripts (e.g. `uv run python3 ...`).
- Avoid using `pip`, `pip3`, `virtualenv`, or other Python packaging tools unless explicitly requested by the user.
